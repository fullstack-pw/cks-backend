#run locally:
# export ENVIRONMENT=development && export LOG_LEVEL=debug && export KUBECONFIG=/home/pedro/.kube/config && export KUBERNETES_SERVICE_PORT=6443 && export KUBERNETES_SERVICE_HOST=https://192.168.1.21 && go build -o /home/pedro/repos/cks-backend/app /home/pedro/repos/cks-backend/src/cmd/server && /home/pedro/repos/cks-backend/app


* move session persistence to redis, it's storing on container so we can't have more than 1 replica to maintain consistency
* enable go tests