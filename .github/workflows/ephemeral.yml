name: PR Ephemeral Environment

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]

permissions:
  contents: write
  pull-requests: write

jobs:
  ephemeral:
    uses: fullstack-pw/pipelines/.github/workflows/ephemeral-environment.yml@main
    with:
      image-name: library/cks-backend
      dev-hostname-placeholder: dev.api.cks.fullstack.pw
      deployment-name: cks-backend
      cypress-spec: cypress/e2e/cks-backend.cy.js
      cypress-env-key: CKS_BACKEND_URL
    secrets: inherit # pragma: allowlist secret
