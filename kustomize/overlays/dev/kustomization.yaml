apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

images:
  - name: registry.fullstack.pw/library/cks-backend
    newTag: a47b6c5260ee368aa8bedaa166c4de0230462006

configMapGenerator:
  - name: cks-backend-config
    behavior: merge
    envs:
      - configmap
